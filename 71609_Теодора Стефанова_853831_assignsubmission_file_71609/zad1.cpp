#include <iostream>
using namespace std;
int number;
if (number < 5 || number>43)
{
	return1;
}
bool isPrime(int number)
{
	int i;
		for (i > 2, i < number, i++)
		{
		if (number%i == 0)
			isPrime = false;
		cout << "Number is not Prime" << endl;

		}
	if isPrime=true
	{
		cout << "Number is Prime" << endl;
	}
void main()
		int i, number, oddSum = 0;
	for (i = 1, i <= number, i++)
	{
		if (i % 2 == 0)
			oddSum = oddSum + i;
		cout << oddSum << endl;
	}
	int chetno(int number)
		int br=0
		if (number % 2 == 0)
		{
		cout << "Chetno" << endl;
		while (number % 2 == 0)
		{
			br++;
			number = number / 2;
			cout << number << endl;
		}
		int main()
		{

			return isPrime;
		}
		system("pause");
		return 0;

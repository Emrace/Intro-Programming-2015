#include <iostream>
using namespace std;
int number;

//you can't have an if statement outside of a function
if (number < 5 || number>43)
{
	return1;
}
bool isPrime(int number)
{
	int i;
		for (i > 2, i < number, i++)
		{
		if (number%i == 0)
			//you can't assign a value to a function - maybe return false? - -0.1
			isPrime = false;
		cout << "Number is not Prime" << endl;

		}
	if isPrime=true
	{
		cout << "Number is Prime" << endl;
	}
	//you have to use curly brackets - -0.1
void main()
		int i, number, oddSum = 0;
	//need explanation for the rest
	for (i = 1, i <= number, i++)
	{
		if (i % 2 == 0)
			oddSum = oddSum + i;
		cout << oddSum << endl;
	}
	int chetno(int number)
		int br=0
		if (number % 2 == 0)
		{
		cout << "Chetno" << endl;
		while (number % 2 == 0)
		{
			br++;
			number = number / 2;
			cout << number << endl;
		}
		int main()
		{

			return isPrime;
		}
		system("pause");
		return 0;
		

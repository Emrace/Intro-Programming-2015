#include <iostream>
using namespace std;
bool hasNumber(int number, int m);
{
	{
		if (number < 10)
			return number;
	
		if (number % 10 == m)
			return hasNumber = true;

	else
		return hasNumber = false;
}
int main()
{
	int hasNumber(int number, int m)
	int number;
	cout << "Number= " << endl;
	cin >> number;
	int m;
	cout << "m= " << endl;
	cin >> m;
	{
		if hasNumber(int number, int m)
			cout << hasNumber = true << endl;
	}
		else
			cout << hasNumber = false << endl;
			system("pause");
				return 0;


}
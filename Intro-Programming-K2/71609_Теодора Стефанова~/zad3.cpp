#include<iostream>
using namespace std;
{
	
		int isValid(int a)
		if (a != = "+"&& a != = "-"&& a != = "*" && a != = "/")

	if (a[0] == "+" || a[0] == "-" || a[0] == "*" || a[0] == "/")
		return isValid = false;

for (int i=0; i < 100; i++)
{
	if (a[i] > 0 && a[i] < 9 && a[i + 1]>0 && a[i + 1] < 9)
		return isValid = false;
}

int main()
{
	char a[100];
	cout << "Symvols: "
	cin >> a;
	if isValid(int a) = true;
	cout << "valid " << endl;
	else cout << "not valid " << endl;


system("pause");
return 0;
}